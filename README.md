# bank-queue-wait-time-prediction
ML pipeline for predicting bank customer wait times — SimPy simulation, feature engineering (lag/rolling/target-encoding), CatBoost + Stacking Ensemble tuned with Optuna, SHAP interpretability, and a live Streamlit dashboard.
